def infer(x: int) -> int:
    
    """
    Simple inference logic.
    This function is intentionally framework-agnostic
    to allow replacement with an actual ML model.
    """
    
    return x * 2